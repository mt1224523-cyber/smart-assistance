<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production: HTTPS only. Cleartext allowed solely for local dev loopback. -->
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!--
      Certificate pinning - à activer en production.
      1) Récupérer l'empreinte SPKI de votre domaine :
         openssl s_client -connect api.exemple.ci:443 -servername api.exemple.ci < /dev/null 2>/dev/null \
           | openssl x509 -pubkey -noout \
           | openssl pkey -pubin -outform DER \
           | openssl dgst -sha256 -binary \
           | openssl enc -base64
      2) Remplacer DOMAIN et les deux pins ci-dessous (primaire + backup obligatoire pour la rotation).
      3) Décommenter le bloc <domain-config> suivant.

    <domain-config>
        <domain includeSubdomains="true">api.exemple.ci</domain>
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">REMPLACER_PAR_EMPREINTE_PRIMAIRE=</pin>
            <pin digest="SHA-256">REMPLACER_PAR_EMPREINTE_BACKUP=</pin>
        </pin-set>
    </domain-config>
    -->
</network-security-config>
