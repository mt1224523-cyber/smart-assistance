fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
pydantic>=2.7,<3.0
# Optionnel : décommenter pour activer Firebase App Check côté serveur.
# firebase-admin>=6.5,<7.0
# Optionnel : crash reporting. Activé automatiquement si SENTRY_DSN est défini.
sentry-sdk[fastapi]>=2.18,<3.0
